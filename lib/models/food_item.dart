import 'package:flutter/material.dart';

class FoodItem {
  final String name;
  final Color color;
  final double price;
  final IconData icon;

  FoodItem(this.name, this.color, this.price, this.icon);
}